export enum DBCollections {
  LOGS = "logs",
  EVENTS = "events",
  USERS = "users",
  LOCATIONS = "locations",
  VIDEOS = "videos",
  TEXT_SURVEYS = "text-surveys",
  SCALE_SURVEYS = "scale-surveys"
}
